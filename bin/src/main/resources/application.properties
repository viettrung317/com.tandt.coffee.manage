spring.application.name=com.tandt.coffee.manage
server.port=8081
# ===============================
# DATABASE CONFIGURATION
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/ttcoffee?useSSL=false&serverTimezone=UTC
spring.datasource.username=trungle
spring.datasource.password=317538
spring.datasource.diver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA CONFIGURATION
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Enable CORS
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# JWT Secret Key
app.jwt.secret=your_secret_key_here
app.jwt.expiration=86400000 # 1 day in milliseconds
